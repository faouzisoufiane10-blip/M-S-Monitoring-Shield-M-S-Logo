# name=systemd/m-s-server.service
[Unit]
Description=M-S Server (Docker managed)
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/opt/m-s
ExecStart=/usr/bin/docker-compose up -d --build
ExecStop=/usr/bin/docker-compose down

[Install]
WantedBy=multi-user.target
